namespace Algorithm.String;

public static class RotateMatrix
{
    public static int Run(int[][] matrix)
    {
        //4 different
        //each second dimension array / 2 lead us to next area 1->2 and 3->4 
        //first dimension array / 2 lead us to next below area 1->3  
        var tempArray = new array[][]
        for(int i = 0; i<matrix.Length){
            
        }        
        
        return 1;
    }
}